#PROIBIDO UTILIZAR ESTE CODIGO NESTE SEM AUTORIZAÇÃO DEVIDA DO DESENVOLVEDOR DO PROJETO!
## CONTATO 11966410273
## EMAIL eduardorns30@gmail.com
## 



##from models.database import SessionLocal
#from sqlalchemy import text

#def reset_geral():
   # db = SessionLocal()
   # try:
        # O PostgreSQL exige o CASCADE se houver chaves estrangeiras
        #db.execute(text("TRUNCATE TABLE movimentacoes, estoque_chapas RESTART IDENTITY CASCADE;"))
      #  db.commit()
       # print("✅ Tudo limpo! Estoque e Histórico zerados.")
  #  except Exception as e:
     #   db.rollback()
      #  print(f"❌ Erro: {e}")
   # finally:
      #  db.close()

#if __name__ == "__main__":
  #  reset_geral()